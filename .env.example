# =============================================================================
# UniFi MCP Server Configuration
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# Server Settings
# -----------------------------------------------------------------------------
PORT=3000
NODE_ENV=development

# Session secret - REQUIRED for secure sessions
# Generate with: openssl rand -hex 32
SESSION_SECRET=your-secret-here

# -----------------------------------------------------------------------------
# Google OAuth Settings
# -----------------------------------------------------------------------------
# These are REQUIRED for authentication. Get them from Google Cloud Console:
#
# 1. Go to: https://console.cloud.google.com/
# 2. Create a new project (or select existing)
# 3. Go to "APIs & Services" > "Credentials"
# 4. Click "Create Credentials" > "OAuth client ID"
# 5. Select "Web application"
# 6. Add authorized redirect URI:
#    - For local dev: http://localhost:3000/auth/google/callback
#    - For production: https://your-domain.com/auth/google/callback
# 7. Copy the Client ID and Client Secret below

GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret

# OAuth callback URL (update for production)
OAUTH_CALLBACK_URL=http://localhost:3000/auth/google/callback

# Comma-separated list of emails allowed to access the server
# Leave empty to allow any Google account (not recommended for production)
ALLOWED_EMAILS=your-email@gmail.com

# -----------------------------------------------------------------------------
# UniFi Controller Settings (for Step 4)
# -----------------------------------------------------------------------------
UNIFI_HOST=192.168.1.1
UNIFI_PORT=443
UNIFI_USERNAME=admin
UNIFI_PASSWORD=your-password
UNIFI_SITE=default
